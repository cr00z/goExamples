package main

import (
	"bufio"
	"fmt"
	"os"
)

func main() {
	var n, a int
	in := bufio.NewReader(os.Stdin)
	fmt.Fscan(in, &n)
	sum := 0
	dins := make([]int)
	for i := 1; i <= n; i++ {
		fmt.Fscan(in, &a)
		if sum < 
	}
}